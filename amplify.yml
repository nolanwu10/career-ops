version: 1
applications:
  - appRoot: apps/web
    frontend:
      buildPath: /
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build --workspace=apps/web
      artifacts:
        baseDirectory: apps/web/.amplify-hosting
        files:
          - "**/*"
